{
  "name": "vibe-node-editor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "deploy": "npm run build && gh-pages -d dist -b gh-pages -m \"deploy: $(node -e \\\"console.log(new Date().toISOString())\\\")\""
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "litegraph.js": "^0.7.18"
  }
}
